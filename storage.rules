rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Couple media files: anyone who knows the Couple ID can read/write.
    match /couples/{coupleId}/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
