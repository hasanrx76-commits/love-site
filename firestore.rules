rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Couple data: anyone who knows the Couple ID can read/write.
    // To make it more secure, replace `true` with an auth check later.
    match /couples/{coupleId}/{document=**} {
      allow read, write: if true;
    }
  }
}
